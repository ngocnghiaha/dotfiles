local colors = require("base46").get_theme_tb "base_30"

return {
  IlluminatedWordText = { bold = true, bg = colors.one_bg3 },
  IlluminatedWordRead = { bold = true, bg = colors.one_bg3 },
  IlluminatedWordWrite = { bold = true, bg = colors.one_bg3 },
}
